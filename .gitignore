# Secrets. sa.json anywhere in the tree, every .env file except the example.
sa.json
*sa.json
*.json.key
.env*
!.env.example
node_modules/
.venv/
.next/
out/
dist/
tmp/
stage0/still.png
stage0/hook.mp4
*.mp4
*.mov
.DS_Store

# D1
service/data/
service/assets/fonts/*.ttf
service/assets/fonts/*.txt
.playwright-mcp/
frames*.json

# Stage 0 scratch binaries. The .mjs scripts stay: they are the verified hook pipeline.
stage0/*.png
stage0/*.jpg
stage0/*.mp4
stage0/*.mov

# Licensed listing photo sets used as local test fixtures. Never committed.
fixtures/

# Next build output
web/.next/
web/out/
